export const selectedLessons=[];export function toggleLesson(e,s=[]){const t=selectedLessons.findIndex(s=>s.date===e.date&&s.hour===e.hour&&s.instructor_id===e.instructor_id);if(t>=0)return selectedLessons.splice(t,1),!1;if(s.includes(Number(e.vehicle_id)))return"duplicate-db";return selectedLessons.some(s=>s.date===e.date&&s.hour===e.hour&&s.vehicle_id===e.vehicle_id)?"duplicate":(selectedLessons.push(e),!0)}export function getLessons(){return selectedLessons}